# Sufficiency Check (2025-11-22T17:27+08:00)
- [x] 清晰接口契约：需要新增图片→歌词接口，输入 image_url（可附加用户要求/风格），输出歌词文本与标题，后端返回 JSON `{"lyrics": "...", "title": "..."}` 并在前端展示后传给 /api/suno/generate。
- [x] 技术选型理解：沿用 FastAPI + DoubaoClient.chat.completions（vision 模型），前端原生 JS 调新接口；无新增依赖。
- [x] 主要风险：模型回复格式不规范导致解析失败；外网/密钥受限无法真实调用；前端回退逻辑缺失导致空歌词。
- [x] 验证方式：本地可构造假响应单测或调用函数 dry-run；前端调用新接口的参数拼装检查；记录无法外网的风险说明。
